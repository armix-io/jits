export type TextAlignVerticalMap = Record<
  "top" | "bottom" | "middle" | "auto",
  string
>;

export const defaultTextAlignVerticalMap: TextAlignVerticalMap = {
  top: "top",
  bottom: "bottom",
  middle: "center",
  auto: "auto",
};
