{
  "name": "@armix/jits-react-native",
  "version": "0.0.0",
  "author": "Armix <opensource@armix.io>",
  "description": "React Native integration for @armix/jits.",
  "repository": "github:armix-io/jits",
  "license": "MIT",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "dev": "yarn clean && yarn watch",
    "local": "yalc publish",
    "clean": "rm -rf lib",
    "build": "yarn build:pkg && yarn build:src",
    "build:src": "tsc",
    "build:pkg": "exit 0",
    "watch": "concurrently -k -n src,pkg yarn:watch:src yarn:watch:pkg",
    "watch:src": "yarn build:src --watch",
    "watch:pkg": "yarn build:pkg && nodemon --exec \"yalc push --replace\"",
    "test": "jest --colors",
    "test:debug": "node --inspect node_modules/.bin/jest --runInBand"
  },
  "peerDependencies": {
    "react": ">=16.8.0",
    "react-native": ">=0.60.0"
  },
  "devDependencies": {
    "@types/react": "^17.0.0",
    "@types/react-native": "^0.64.0",
    "react": "^17.0.0",
    "react-native": "^0.64.0"
  }
}
